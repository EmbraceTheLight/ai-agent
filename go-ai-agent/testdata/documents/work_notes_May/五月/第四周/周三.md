# 周三
## 上午
*   修复/更新测试用例

## 下午

*   **未识别的符号**：以模型 `ThermofluidStream.Examples.WaterHammer` 为例，平坦化后仿真时，编译后端报错："未识别的符号  reservoir.Medium.reference\_X"，查看编译前端扁平化 json 结构文件，发现没有 `reservoir.Medium.reference_X` 这个变量/参数
*   输出了一份**操作手册**，旨在说明通过 dymola 如何加密 & 授权第三方库

## 晚上

完成了 No.76 最小覆盖子串